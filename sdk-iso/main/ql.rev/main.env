#!/usr/bin/bash
export INTSALL_SCRIPT="/installer"
export SDK_PLATFORM="/usr/local/sdk"

export VERSION="1.1"
export RED='\033[0;31m'
export GREEN='\033[0;32m'
export YELLOW='\033[1;33m'
export BLUE='\033[0;34m'
export PURPLE='\033[0;35m'
export CYAN='\033[0;36m'
export NC='\033[0m'
export BRIGHT_WHITE='\033[1;97m$*\033[0m'

info() { echo -e "${BLUE}[info]${NC} $1"; }
error() { echo -e "${RED}[error]${NC} ${1}                                           :("; }
success() { echo -e "${GREEN}[success]${NC} $1"; }

echo_m() {
    cols=$(tput cols)
    text="$1"

    # Цветной текст через echo -e (без центрирования)
    colored_text=$(echo -e "\033[1;97m${text}\033[0m")

    # Считаем длину только текста (без цветных кодов)
    text_length=${#text}

    # Центрируем и выводим цветной текст
    printf "%*s\n" $(( ($cols + text_length) / 2 )) "$colored_text"
}
